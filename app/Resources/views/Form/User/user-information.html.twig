{{ form_start(form, {
    'action': path('user_information_edit_ajax'),
    'attr': {
        'id': 'ajax-form-user-information-edit',
        'class': 'col p-0'
    }
}) }}

{% include ':FlashAlert:flash-alert.html.twig' with {
    'alertType': 'success',
    'alertStyle': 'success',
    'textAlign': 'center'
} %}

<div class="row">
    <div class="col mt-3">
        {{ form_label(form.username) }}
        {{ form_widget(form.username) }}
    </div>
</div>

<div class="row">
    <div class="col mt-3">
        <input type="submit" value="{{ 'global.form.save_changes'|trans }}"/>
    </div>
</div>
{{ form_end(form) }}
